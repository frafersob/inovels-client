import { User } from '../user/user';
export class Image {
        id: number;
        user: User;
        src: string;
        sizeX: number;
        sizeY: number;
        offsetX: number;
        offsetY: number;
}
